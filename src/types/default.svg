<svg
  xmlns="http://www.w3.org/2000/svg"
  xmlns:xlink="http://www.w3.org/1999/xlink"
  width="{{width}}"
  height="{{height}}">

    <defs>
      <style type="text/css">
         @font-face {
           font-family: {{font.name}};
           src: url({{font.url}});
         }
       </style>
    </defs>

    <rect rx="10" ry="10" width="{{width}}" height="{{height}}" style="fill:#f0f0f0;stroke-width:5;stroke:#999999;" />

    <g fill="#33333" text-anchor="middle" font-family="{{font.name}},sans-serif" font-size="{{font.size}}">
      {{content}}
    </g>

</svg>
